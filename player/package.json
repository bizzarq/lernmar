{
  "name": "lernmar-player",
  "version": "0.1.0",
  "description": "A player for SCORM courses",
  "author": "Bizzarq Software",
  "license": "MIT",
  "main": "main.ts",
  "typings": "src/main.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "python -m http.server -d dist/ || python3 -m http.server -d dist/"
  },
  "dependencies": {
    "lernmar-api": "file:../api"
  }
}
